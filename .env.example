# vibevibe 的环境变量
#
# 这个项目**不使用任何密钥**:全程离线推理,没有 API key、没有账号、
# 不发一个网络请求。所以这里只有一个可选的路径覆盖。

# 配置文件位置。留空则按下面的顺序查找:
#   1. ~/.config/vibevibe/config.toml
#   2. 包内自带的模板(全默认值)
# VIBEVIBE_CONFIG=/path/to/config.toml
